{

    "domain": "http://localhost:3000",
    "port": 3000,
    "debugMode": false,
    "seoKeywords": "help, desk, helpdesk, information, support, system",
    "ownerIds": ["YOUR_USER_ID"],

    "sql": {
        "host": "localhost",
        "user": "root",
        "password": "",
        "database": "supportsystem"
    },

    "discord": {
        "enabled": false,
        "oauthId": "YOUR_OAUTH_CLIENT_ID",
        "oauthToken": "YOUR_OAUTH_CLIENT_SECRET"
    },

    "timeZone": {
        "tz": "EST",
        "format": "MM-DD-YYYY hh:mm A"
    }

}
